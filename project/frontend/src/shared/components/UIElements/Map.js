import React from 'react';

import './Map.css';

const Map = () => {
    return (
        <div className={`map ${PaymentResponse.className}`} style={props.style}>
            <h1>DUMMY_MAP</h1>
        </div>
    );
};

export default Map;